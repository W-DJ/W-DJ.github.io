<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="often">

<select id="select_list" resultType="pack.spring.domain.often.OftenDTO">
   <![CDATA[
	 select * from OftenTbl order by num desc
 	]]>
	</select>
	

<insert id="insert" parameterType="hashMap" useGeneratedKeys="true" keyProperty="often">
<![CDATA[
insert into OftenTbl (aId, aName, subject, qnaType , content, regTM) values
 (#{aId}, #{aName}, #{subject}, #{qnaType},#{content},now());
]]>
</insert>
</mapper>